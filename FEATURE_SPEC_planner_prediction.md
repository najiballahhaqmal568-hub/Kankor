# Feature Spec — برنامه‌ریز هوشمند مطالعه + پیش‌بینی قبولی + داستان‌های موفقیت
نسخه: ۱.۰ | تاریخ: سرطان ۱۴۰۵ | وضعیت: طراحی‌شده، در انتظار فاز ساخت (بعد از رسیدن بانک سؤال به ۳۰۰+)

---

## ۱. چشم‌انداز

اپ از «ابزار تمرین» به «مربی شخصی کانکور» تبدیل می‌شود: برای هر شاگرد بر اساس
رشته هدف، وقت موجود، و سطح فعلی، یک برنامه مطالعاتی زنده می‌سازد؛ پیشرفت را
پیگیری می‌کند؛ با داده‌های واقعی نتایج سال‌های گذشته شانس قبولی را پیش‌بینی
می‌کند؛ و با داستان‌های موفقیت انگیزه می‌دهد.

الگوی الهام: چرخه اثبات‌شده آموزشگاه‌های حضوری —
**تدریس → تمرین → تست فردا → درجه‌بندی → توجه به ضعف** — به‌صورت خودکار و شخصی.

### فلسفه تجاری (ستاره قطبی محصول)
**«آموزشگاه را دیجیتالی می‌سازیم»** — شاگرد باید بتواند فقط با گوشی خود، به رشته
دلخواهش کامیاب شود. هدف: اپ آن‌قدر در مسیر موفقیت شاگرد نقش واقعی داشته باشد
که او آن را جزء جدایی‌ناپذیر آمادگی‌اش بداند و به هر هم‌صنفی معرفی‌اش کند.

این «ضروری‌بودن» باید از **ارزش واقعی** بیاید نه وابستگی مصنوعی:
- هر قابلیت باید مستقیماً نمره شاگرد را بالا ببرد، وگرنه ساخته نمی‌شود
- معیار موفقیت محصول: «چند شاگرد با ما به رشته هدف‌شان رسیدند» —
  نه تعداد دانلود، نه دقیقه‌های استفاده
- هر تصمیم طراحی با این سؤال سنجیده می‌شود:
  **«آیا معلم خوب آموزشگاه همین کار را می‌کرد؟»**

---

## ۲. لایه ۱ — ورودی‌های برنامه (آنبوردینگ)

هنگام اولین ورود (قابل ویرایش بعدی در تنظیمات):

| ورودی | گزینه‌ها | استفاده |
|---|---|---|
| رشته هدف | طب / ستوماتولوژی / فارمسی / انجنیری / کمپیوتر ساینس / اقتصاد / حقوق / شرعیات / سایر | وزن‌دهی مضامین + هدف نمره |
| دانشگاه هدف | لیست از داده cutoffs (کابل، ننگرهار، بلخ، هرات...) | هدف نمره دقیق |
| تاریخ تقریبی کانکور | ماه/سال | طول برنامه |
| ساعت مطالعه روزانه | ۱ / ۲ / ۳ / ۴+ ساعت | حجم روزانه |
| وضعیت | صنف ۱۱ / صنف ۱۲ / فارغ | نقطه شروع نصاب |
| حالت مطالعه | مستقل (برنامه کامل) / کورسی (مکمل کورس) | شدت برنامه |
| آزمون تشخیصی اولیه | ~۴۰ سؤال مخلوط | سطح‌سنجی اولیه هر مضمون |

**نمایش تعهد:** قبل از شروع، کل برنامه (تقویم ماه‌به‌ماه) به شاگرد نشان داده
می‌شود و او تأیید می‌کند: «از این به بعد طبق این برنامه پیش می‌روی.»
(اصل روانشناسی تعهد — ایده نجیب)

---

## ۳. لایه ۲ — موتور تولید برنامه

### ۳.۱ وزن‌دهی مضامین بر اساس رشته
هر رشته یک پروفایل وزنی دارد (قابل تنظیم در فایل config):

```
طب:            بیولوژی ۳۵٪، کیمیا ۲۵٪، فزیک ۱۵٪، ریاضی ۱۰٪، حفظیات ۱۵٪
انجنیری:       ریاضی ۳۵٪، فزیک ۳۰٪، کیمیا ۱۵٪، حفظیات ۲۰٪
کمپیوتر ساینس: ریاضی ۴۰٪، فزیک ۲۰٪، انگلیسی ۱۵٪، حفظیات ۲۵٪
اقتصاد/حقوق:   ریاضی ۲۰٪، حفظیات (تاریخ/جغرافیه/دری) ۵۰٪، ...
```
(نکته: کانکور افغانستان نمره کل واحد دارد نه ضریب رسمی رشته‌ای؛ این وزن‌ها
«استراتژی بهینه مطالعه» است بر اساس سهم سؤالات هر مضمون در فورم + سطح فعلی
شاگرد، نه ضریب رسمی. باید در UI شفاف گفته شود.)

### ۳.۲ دو نوع محتوا، دو نوع برنامه‌ریزی
- **مضامین تمرینی (ریاضی/فزیک/کیمیا):** روزانه درس + تمرین → تست همان مبحث
  فردا (الگوی آموزشگاه). واحد: مبحث.
- **مضامین حفظی (تاریخ/جغرافیه/دینیات/ادبیات/بیولوژی-حفظی):** تقسیم صفحات کتاب
  بر روزهای موجود (مثلاً ۱۰-۲۰ صفحه/روز) → تست همان صفحات فردا. واحد: صفحه/فصل.

### ۳.۳ الگوریتم تقسیم (بدون AI — قابل‌اجرا آفلاین)
```
روزهای باقی‌مانده = تاریخ کانکور − امروز − ۳۰ (ماه آخر رزرو)
بودجه روزانه (دقیقه) = ساعت اعلام‌شده × ۶۰
سهم هر مضمون = بودجه × وزن رشته × ضریب ضعف شاگرد
  (ضریب ضعف از آزمون تشخیصی: مضمون ضعیف‌تر = سهم بیشتر)
خروجی: تقویم روزانه → [مضمون، مبحث/صفحات، نوع فعالیت (درس/تمرین/تست/مرور)]
```
+ مرور فاصله‌دار موجود (SM-2) به‌عنوان بخش ثابت روزانه در برنامه جا می‌گیرد.

### ۳.۵ استراتژی تخصیص وقت: ضعف یا قوت؟ (ایده نجیب)
اصل: جهت تمرکز برنامه به **زمان باقی‌مانده** بستگی دارد، نه فقط به نقاط ضعف.

مباحث شاگرد بر اساس آزمون‌ها به سه دسته تقسیم می‌شوند:
- 🔴 **ضعیف** (زیر ۴۰٪ درست) — یادگیری از پایه لازم دارد، پرهزینه
- 🟡 **نزدیک به تسلط** (۴۰-۷۰٪) — با کمی کار به نمره تبدیل می‌شود، بهترین بازده
- 🟢 **قوی** (بالای ۷۰٪) — فقط نگهداری با مرور فاصله‌دار

قانون سوئیچ خودکار بر اساس زمان تا کانکور:
```
بیش از ۳ ماه مانده:   ۵۰٪ ضعیف + ۳۰٪ نزدیک‌به‌تسلط + ۲۰٪ نگهداری قوی
۱ تا ۳ ماه مانده:     ۲۵٪ ضعیف + ۵۰٪ نزدیک‌به‌تسلط + ۲۵٪ نگهداری قوی
کمتر از ۱ ماه:        ۰٪ ضعیف جدید + ۵۰٪ نزدیک‌به‌تسلط + ۵۰٪ تثبیت قوی
```
منطق (به شاگرد هم شفاف گفته می‌شود): «وقتی وقت کم است، مبحث کاملاً جدید
یادگرفتن گران‌ترین کار است — همان چیزی که بلدی را محکم کن و نمره‌اش را
تضمین کن.» در حالت وقت‌کم، مباحث ضعیف از برنامه حذف نمی‌شوند بلکه به
«بعد از کانکور / سال بعد» منتقل و به شاگرد اعلام می‌شود تا احساس
پنهان‌کاری نشود.

استثنا: اگر مبحث ضعیف، وزن بسیار بالایی در رشته هدف دارد (مثلاً بیولوژی
برای طب)، حتی در وقت کم حداقل سهم کوچکی نگه داشته می‌شود — با هشدار
واقع‌بینانه درباره تأثیرش بر نمره تخمینی.

### ۳.۴ حالت کورسی
اگر شاگرد «کورسی» انتخاب کند: برنامه فقط شامل (الف) تست روزانه از مباحثی که
خودش علامت می‌زند «امروز در کورس خواندم»، (ب) مرور فاصله‌دار، (ج) آزمون‌های
شبیه‌سازی. هیچ تدریس/تقسیم صفحه تحمیل نمی‌شود.

---

## ۴. لایه ۳ — پیگیری، هشدار، انگیزه

### ۴.۱ داشبورد پیشرفت
- «امروز: ۳ وظیفه» + تیک زدن
- نوار پیشرفت کلی: «٪ از برنامه طی‌شده» در برابر «٪ زمان گذشته»
- وضعیت: 🟢 طبق برنامه / 🟡 کمی عقب (۱-۳ روز) / 🔴 عقب‌افتاده (۴+ روز)

### ۴.۲ هشدارها و تنظیم مجدد
- ۲ روز بی‌فعالیتی → نوتیفیکیشن ملایم
- ۴+ روز عقب → پیشنهاد «بازچینش خودکار برنامه» (فشرده‌سازی یا حذف مباحث کم‌اهمیت‌تر)
- هرگز شرمنده‌سازی نکن؛ لحن: «برگردیم به مسیر» نه «تنبل شدی»

### ۴.۳ هویت انگیزشی (ایده نجیب)
- شاگرد با عنوان رشته هدفش خطاب می‌شود: **«داکتر احمد»، «انجنیر نجیب»**
- در هدر داشبورد، کارت هدف: «هدف: طب کابل — نمره لازم: ۳۴۸»
- پیام‌های نقطه عطف: «نیم راه را آمدی، داکتر!»

### ۴.۴ پیش‌بینی نمره و شانس قبولی (داده‌محور)
منبع: `kankor_1405_cutoffs.json` (تولیدشده از ۹۴,۶۸۷ نتیجه واقعی ۱۴۰۵؛ هر سال
فایل سال جدید اضافه می‌شود و میانگین متحرک ۲-۳ سال ملاک می‌شود).

- نمره تخمینی شاگرد = برون‌یابی از نتایج آزمون‌های شبیه‌سازی
  (فورمول کانکور: هر سؤال درست ≈ نمره مثبت؛ مقیاس به ۳۶۰)
- مقایسه با cutoff رشته/دانشگاه هدف:
  - «نمره تخمینی تو: ۲۹۵ | حداقل قبولی طب کابل ۱۴۰۵: ۳۴۸ → فاصله: ۵۳ نمره»
  - پیشنهاد جایگزین واقع‌بینانه: «با همین نمره: طب پکتیا (۳۳۴)؟ فارمسی بلخ (۳۰۳)؟»
- ⚠️ قواعد صداقت:
  - همیشه بنویس «بر اساس نتایج ۱۴۰۵؛ نمرات هر سال تغییر می‌کند»
  - هرگز وعده قطعی نده؛ بازه بده نه عدد واحد
  - سهمیه‌های ولایتی جدا هستند و در cutoff عمومی حساب نشده‌اند — ذکر شود

### ۴.۵ داستان‌های موفقیت (ایده نجیب)
- بعد از اعلام نتایج هر سال: شاگردانی که با اپ آماده شدند و قبول شدند،
  با **رضایت خودشان** عکس + نمره + رشته قبولی‌شان در بخش «ستاره‌های ما» نمایش
  داده می‌شود («احمد از غور — طب کابل — ۳۵۱»)
- الزامات: فورم رضایت صریح؛ امکان حذف در هر زمان؛ بدون نمایش معلومات تماس
- فاز اول (قبل از داشتن قبولی واقعی): می‌توان از آمار عمومی ۱۴۰۵ استفاده کرد
  («۸۷ نفر امسال طب کابل قبول شدند — نفر بعدی تو باش»)

---

## ۵. لایه ۴ — ماه سرنوشت‌ساز (حالت دوره نهایی)

فعال‌سازی خودکار ۳۰ روز مانده به کانکور:
- برنامه عادی متوقف؛ حالت «دوره کامل» شروع
- چرخه: دوره سریع مضمون → آزمون شبیه‌سازی کامل (۱۶۰ سؤاله، ۱۸۰ دقیقه) → تحلیل → تمرکز روی ضعیف‌ترین‌ها → تکرار
- تعداد شبیه‌سازی‌ها تطبیقی: پایه ۴ دور؛ اگر نوسان نمره بالا یا ضعف زیاد → تا ۸ دور
- اولین شبیه‌سازی از پایان «دور اول برنامه» (نه فقط ماه آخر) وارد تقویم می‌شود — ایده نجیب
- روز آخر: فقط مرور فورمول‌ها و فلش‌کارت‌ها؛ بدون مطلب جدید (اصل علمی)

---

## ۶. داده‌ها و فایل‌ها

| فایل | محتوا | به‌روزرسانی |
|---|---|---|
| `kankor_1405_cutoffs.json` | ۳۳۴ رشته-دانشگاه: cutoff/میانگین/سقف/تعداد | سالانه (نجیب فایل نتایج را می‌دهد) |
| `field_weights.json` | وزن مضامین هر رشته | دستی، با بازخورد معلمان |
| `plan_state` (SQLite) | برنامه شاگرد + پیشرفت روزانه | لحظه‌ای، آفلاین |
| `success_stories.json` | داستان‌های تأییدشده | بعد از هر اعلام نتایج |

---

## ۷. نقشه ساخت (فازبندی)

| فاز | محتوا | پیش‌نیاز |
|---|---|---|
| v0.2 | لایه ۱ + ۲ (آنبوردینگ، تولید تقویم، حالت کورسی) | بانک ۳۰۰+ سؤال |
| v0.3 | لایه ۳ (پیگیری، هشدار، پیش‌بینی نمره با cutoffs، هویت انگیزشی) | v0.2 |
| v0.4 | لایه ۴ (ماه سرنوشت‌ساز، شبیه‌سازی تطبیقی) + داستان‌های موفقیت | v0.3 + کاربران واقعی |

---

## ۸. یافته‌های کلیدی داده ۱۴۰۵ (مرجع سریع)

- شرکت‌کنندگان کانکور عمومی: ۸۸,۹۹۷ | قبولی دانشگاه دولتی: ۵۱.۷٪
- سخت‌ترین: طب کابل (ابن سینا) cutoff=۳۴۸ | هرات ۳۴۱.۶ | ننگرهار ۳۴۰.۹
- کمپیوتر ساینس کابل: ۳۲۰.۴ | فارمسی کابل: ۳۱۴.۵ | ستوماتولوژی کابل: ۳۳۰.۹
- نمره زیر ۱۵۰ → شانس تقریباً صفر | بالای ۳۲۰ → ۸۵.۵٪
- باند ۱۵۰-۱۸۰ بزرگ‌ترین گروه (۴۰,۴۱۶ نفر) با قبولی فقط ۳۶.۹٪ —
  **بزرگ‌ترین بازار اپ: شاگردانی که با ۲۰-۳۰ نمره بهبود، سرنوشت‌شان عوض می‌شود**

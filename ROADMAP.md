# 🗺️ نقشه راه — اپ آمادگی کانکور

## فاز ۱ — MVP آفلاین (در جریان)

### مرحله ۱ — بنیاد ✅
- [x] Scaffold پروژه Vite + React + TypeScript
- [x] سه فایل ماندگار (NOTEBOOK / ROADMAP / DECISIONS)
- [x] توکن‌های رنگ لاجورد + زعفرانی، تم دارک (پیش‌فرض) و روشن
- [x] RTL کامل + فونت Vazirmatn لوکال
- [x] پوسته اپ: هدر، ناوبری پایین، مسیرهای صفحات

### مرحله ۲ — مدل داده و بانک سؤالات ✅
- [x] `schema.ts`: تایپ‌های Question / Subject / ExamBlueprint / Attempt / SrsCard / ActiveExam
- [x] `subjects.ts`: ۱۰ مضمون کانکور + وزن هر مضمون در آزمون کامل (۱۶۰س/۱۸۰د)
- [x] بانک سؤالات نمونه (۶۵ سؤال، ۱۰ مضمون) با توضیح هر ۴ گزینه
- [x] Dexie: جداول questions / attempts / answers / srsCards / activeExam / meta
- [x] Seed نسخه‌دار بانک سؤالات به IndexedDB + اعتبارسنجی

### مرحله ۳ — صفحه بانک سؤالات ✅
- [x] فیلتر مضمون / مبحث / سختی (chip‌ها، مبحث وابسته به مضمون)
- [x] حالت تمرین: بازخورد فوری + توضیح هر ۴ گزینه + پیشنهاد مطالعه
- [x] ورود خودکار جواب‌های غلط به صف مرور (`enqueueForReview`)
- [x] کامپوننت مشترک `QuestionView` (قابل استفاده در کارنامه و مرور)

### مرحله ۴ — شبیه‌ساز آزمون ✅
- [x] موتور آزمون خالص (`examEngine.ts`): buildExam + gradeExam + تست واحد (۷ تست)
- [x] ExamSetup: آزمون کامل (۱۶۰س/۱۸۰د) / مضمونی / سفارشی
- [x] ExamRunner: تایمر بر پایه deadline، شبکه ناوبری، پرچم‌گذاری
- [x] ثبت خودکار در پایان وقت + ادامه آزمون نیمه‌کاره + ثبت زودهنگام
- [x] غلط‌ها هنگام ثبت آزمون خودکار وارد صف مرور می‌شوند

### مرحله ۵ — کارنامه هوشمند ✅
- [x] `analytics.ts` خالص + ۵ تست واحد (topicBreakdown, weakestTopics, studyRecommendations)
- [x] نمره کل + کاشی‌های آماری + پیام کلی + تاریخ شمسی
- [x] نمودار میله‌ای مضمون‌به‌مضمون با رنگ‌بندی وضعیتی (سبز/زعفرانی/سرخ + برچسب عددی)
- [x] ضعیف‌ترین مباحث
- [x] مرور سؤال‌به‌سؤال با توضیح هر ۴ گزینه
- [x] پیشنهاد مطالعه گروه‌بندی‌شده بر اساس مضمون

### مرحله ۶ — مرور فاصله‌دار ✅
- [x] الگوریتم SM-2 خالص (`sm2.ts`) + ۷ تست واحد (کف EF، ریست، فواصل)
- [x] `gradeCard`/`dueCards` روی IndexedDB
- [x] صفحه Review: صف سررسید، فلش‌کارت، دکمه‌های کیفیت (سخت/خوب/آسان + دوباره)
- [x] حالت خالی و پایان جلسه

### مرحله ۷ — PWA و صیقل نهایی ✅
- [x] vite-plugin-pwa + مانیفست RTL + آیکون‌های ۱۹۲/۵۱۲/maskable
- [x] تست آفلاین کامل (Service Worker + رفرش در حالت آفلاین)
- [x] صفحه تنظیمات (تم، اندازه فونت، پاک‌کردن پیشرفت)
- [x] داشبورد نهایی Home (کارت‌های آماده مرور، آخرین کارنامه، آمار زنده)

## فاز ۱ کامل شد 🎉
همه قابلیت‌های MVP آفلاین پیاده و به‌صورت end-to-end تست شدند.

### انتشار ✅
- [x] Workflow انتشار خودکار GitHub Pages (`deploy-pages.yml`)
- [x] فعال‌سازی Pages توسط مالک مخزن (Source: GitHub Actions)
- [x] **اپ زنده:** https://najiballahhaqmal568-hub.github.io/Kankor/

### مرحله ۸ — بانک سؤالات واقعی (رهنمون ۱۴۰۵) ✅
- [x] جایگزینی بانک نمونه با `questions.json` واقعی (۵۷ سؤال verified، ریاضی/فزیک/کیمیا/بیولوژی)
- [x] مهاجرت کامل schema به ساختار داده (بدون adapter، بدون فیلد اضافه) — `getCorrectIndex(q)`
- [x] فیلتر verified فقط در پروداکشن
- [x] مضمون/سختی پویا از داده (نه enum ثابت) — بچ‌های بعدی بدون تغییر کد اضافه می‌شوند
- [x] نمایش `explanation` + `why_correct`/`why_wrong` هر گزینه؛ برای پاسخ غلط، توضیح گزینهٔ انتخابی برجسته می‌شود
- [x] کارنامه: تفکیک تودرتوی مضمون → مبحث (`subjectTopicBreakdown`)
- [x] به‌روزرسانی تست‌های analytics/examEngine با ساختار جدید (۱۹ تست سبز)

### مرحله ۹ — ابزار ادغام بچ‌های سؤال + بچ دوم ریاضی ✅
- [x] `scripts/merge-batch.mjs`: نُرمال‌سازی خودکار دو شکل خام شناخته‌شده + یکدست‌سازی نام مضمون/سختی + رد تکراری‌ها بر اساس id
- [x] ادغام بچ‌های دوم تا ششم (۱۳+۲۱+۹ ریاضی، ۷ کیمیا/بیولوژی، ۹ کیمیا/فزیک رهنمون) — بانک اکنون ۱۱۶ سؤال

### مرحله ۱۰ — جریان کاری `content/questions` + اعتبارسنجی سخت‌گیرانه ✅
- [x] پوشه `content/questions/` به‌عنوان محل فایل‌های خام بچ‌ها + آرشیو هر ۶ بچ قبلی
- [x] اجرای بدون آرگومان: فایل‌های جدید/تغییرکرده خودکار پیدا می‌شوند (هش در `.merged.json`)
- [x] اعتبارسنجی خط دفاع دوم: correct_index غیر null و در محدوده، دقیقاً ۴ گزینه، `verified:true`، توضیح کامل همه گزینه‌ها
- [x] گزارش هر سؤال ردشده با دلیل دقیق — سؤال ردشده هرگز وارد بانک نمی‌شود
- [x] راهنمای `content/questions/README.md` برای مشارکت‌کنندگان محتوا

### مرحله ۱۱ — رفع یافته‌های بازبینی دو-محورهٔ کد ✅
- [x] **ADR-010:** جداسازی `topic` (دستهٔ درشت) از `chapter_ref` (زیرمبحث ریز) در `merge-batch.mjs` + بازنگاشت ۵۹ سؤال قبلی → مباحث ۱۰۲→۷۵، تک‌سؤالی ۹۰→۵۴، ریاضیات ۵۵→۳۲
- [x] `topic` و برابری طول `why_wrong` با تعداد گزینه‌ها به دروازهٔ اعتبارسنجی اضافه شد
- [x] **ADR-011:** ارتقای خودکار `BANK_VERSION` هنگام ادغام (دیگر هیچ تغییر کد دستی برای بچ جدید لازم نیست)
- [x] حذف نشت هم‌معنی «ریاضی» از `subjects.ts` و fixture تست‌ها (ADR-008: هم‌معنی‌ها فقط در اسکریپت ادغام)
- [x] حذف «۱۶۰ سؤال · ۱۸۰ دقیقه» هاردکد‌شده از `ExamSetup` — از `FULL_EXAM_TOTAL`/`FULL_EXAM_DURATION_SEC` خوانده می‌شود (ADR-006)
- [x] هم‌تراز‌کردن `validateBank` با دروازهٔ ادغام (دقیقاً ۴ گزینه + `topic`) — دو اعتبارسنجی دیگر واگرا نیستند
- [x] `QuestionView`: توضیح تکراری گزینهٔ درست وقتی عیناً برابر `explanation` است چاپ نمی‌شود
- [x] بازگرداندن «ضعیف‌ترین مباحث» به کارنامه (`weakestTopics` کاربر واقعی نداشت)

### مرحله ۱۲ — قفل‌شدن شالودهٔ دادهٔ برنامه‌ریز (تصمیم، نه کد) ✅
- [x] ADR-012: پیش‌بینی قبولی از v0.3 حذف و به v0.4 موکول شد (بدون فورمول رسمی، عدد تخمینی نمی‌دهیم)
- [x] ADR-013…۰۱۶: درخت نصاب جدا تألیف می‌شود · مضمون → کتاب صنفی (۱۰/۱۱/۱۲) → فصل/درس · نشانِ نوع تمرینی/حفظی · واحد حفظی «عنوان درس» + صفحهٔ وابسته به چاپ
- [x] ADR-017، ۰۱۸: `plan_state` = سربرگ تعهد + برگه‌های وظیفهٔ مستقل؛ بازچینی هیچ برگه‌ای را پاک نمی‌کند
- [x] ADR-019، ۰۲۰: اولویت ساینس با لحن استراتژیک (بدون ادعای ضریب رسمی) · تخصیص دو مرحله‌ای با اصلاح کران‌دار ضعف
- [x] تکت‌های wayfinder ۰۰۲، ۰۰۳، ۰۰۶ بسته شدند · ۰۰۴ آزاد شد · ۰۰۵ تا v0.4 معلق

### در انتظار شروع v0.2 — دو پیش‌نیاز، هیچ‌کدام تصمیم نیستند
- [ ] تألیف `curriculum.json` (تکت ۰۰۴) + اعتبارسنجی سوم در `merge-batch.mjs`
- [ ] رساندن بانک به ۳۰۰+ سؤال، به‌ویژه مضامین انسانی که هنوز صفر سؤال دارند

## فاز ۲ — آینده (خارج از MVP)
- بسته‌بندی APK اندروید با Capacitor
- بچ‌های بعدی سؤالات (مضامین انسانی: دری/پشتو/انگلیسی/دینیات/تاریخ/جغرافیه)
- زبان پشتو در رابط
- همگام‌سازی ابری اختیاری و رتبه‌بندی

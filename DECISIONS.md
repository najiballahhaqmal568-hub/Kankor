# 🧭 تصمیم‌های معماری (ADR)

> هر تصمیم مهم با دلیل و بدیل‌های ردشده ثبت می‌شود. تصمیم‌های جدید به انتها اضافه شوند.

---

## ADR-001 — پلتفرم: PWA (وب‌اپ آفلاین)
**تاریخ:** ۱۴۰۵/۰۴/۲۰ (2026-07-11) · **وضعیت:** تأییدشده توسط کاربر

**تصمیم:** فاز ۱ به‌صورت PWA ساخته می‌شود؛ فاز ۲ با Capacitor به APK تبدیل خواهد شد.

**دلیل:** کاملاً آفلاین با Service Worker؛ قابل نصب روی موبایل؛ در محیط توسعه فعلی قابل تست و پیش‌نمایش؛ مسیر مهاجرت ساده به اندروید.

**بدیل‌های ردشده:** Flutter (پیش‌نمایش زنده در محیط ابری ممکن نبود)، React Native/Expo (وابسته به سرویس بیلد ابری).

---

## ADR-002 — ذخیره‌سازی: JSON استاتیک + IndexedDB (Dexie)
**وضعیت:** پذیرفته

**تصمیم:** محتوای سؤالات به‌صورت فایل‌های JSON همراه اپ حمل و در اولین اجرا (نسخه‌دار) به IndexedDB وارد می‌شود. وضعیت کاربر (نتایج، کارت‌های SM-2، تنظیمات) فقط در IndexedDB نگه‌داری می‌شود.

**دلیل:** IndexedDB معادل استاندارد SQLite در وب است؛ Dexie API تمیز و indexed query می‌دهد؛ JSON استاتیک، افزودن سؤالات جدید را به یک PR ساده تبدیل می‌کند.

**بدیل ردشده:** sql.js/wa-sqlite (حجم WASM ~۱MB و پیچیدگی persistence بدون مزیت واقعی برای این حجم داده).

---

## ADR-003 — استایل: CSS خالص با متغیر، بدون فریمورک
**وضعیت:** پذیرفته

**تصمیم:** توکن‌های طراحی در `tokens.css`؛ بدون Tailwind/UI-kit.

**دلیل:** کنترل کامل RTL و تم دوگانه؛ حجم باندل کوچک (مهم برای موبایل‌های افغانستان)؛ پالت سفارشی لاجورد/زعفرانی.

---

## ADR-004 — مسیریابی: HashRouter + `base: './'`
**وضعیت:** پذیرفته

**دلیل:** خروجی استاتیک روی هر هاستی (GitHub Pages، فایل لوکال، WebView اندروید) بدون تنظیم سرور کار می‌کند.

---

## ADR-005 — فونت: Vazirmatn باندل‌شده لوکال
**وضعیت:** پذیرفته

**تصمیم:** فقط وزن‌های 400/500/700 از پکیج npm با `@font-face` باندل می‌شود.

**دلیل:** شرط آفلاین کامل — هیچ CDN ای مجاز نیست؛ Vazirmatn پشتیبانی عالی دری/پشتو و ارقام فارسی (ss01) دارد.

---

## ADR-006 — ساختار آزمون کامل: ۱۶۰ سؤال / ۱۸۰ دقیقه
**وضعیت:** پذیرفته (قابل تنظیم)

**تصمیم:** blueprint آزمون کامل در `subjects.ts` به‌صورت داده تعریف می‌شود، نه هاردکد — تا با تغییر ساختار رسمی کانکور فقط یک فایل عوض شود.

# 🧭 تصمیم‌های معماری (ADR)

> هر تصمیم مهم با دلیل و بدیل‌های ردشده ثبت می‌شود. تصمیم‌های جدید به انتها اضافه شوند.

---

## ADR-001 — پلتفرم: PWA (وب‌اپ آفلاین)
**تاریخ:** ۱۴۰۵/۰۴/۲۰ (2026-07-11) · **وضعیت:** تأییدشده توسط کاربر

**تصمیم:** فاز ۱ به‌صورت PWA ساخته می‌شود؛ فاز ۲ با Capacitor به APK تبدیل خواهد شد.

**دلیل:** کاملاً آفلاین با Service Worker؛ قابل نصب روی موبایل؛ در محیط توسعه فعلی قابل تست و پیش‌نمایش؛ مسیر مهاجرت ساده به اندروید.

**بدیل‌های ردشده:** Flutter (پیش‌نمایش زنده در محیط ابری ممکن نبود)، React Native/Expo (وابسته به سرویس بیلد ابری).

---

## ADR-002 — ذخیره‌سازی: JSON استاتیک + IndexedDB (Dexie)
**وضعیت:** پذیرفته

**تصمیم:** محتوای سؤالات به‌صورت فایل‌های JSON همراه اپ حمل و در اولین اجرا (نسخه‌دار) به IndexedDB وارد می‌شود. وضعیت کاربر (نتایج، کارت‌های SM-2، تنظیمات) فقط در IndexedDB نگه‌داری می‌شود.

**دلیل:** IndexedDB معادل استاندارد SQLite در وب است؛ Dexie API تمیز و indexed query می‌دهد؛ JSON استاتیک، افزودن سؤالات جدید را به یک PR ساده تبدیل می‌کند.

**بدیل ردشده:** sql.js/wa-sqlite (حجم WASM ~۱MB و پیچیدگی persistence بدون مزیت واقعی برای این حجم داده).

---

## ADR-003 — استایل: CSS خالص با متغیر، بدون فریمورک
**وضعیت:** پذیرفته

**تصمیم:** توکن‌های طراحی در `tokens.css`؛ بدون Tailwind/UI-kit.

**دلیل:** کنترل کامل RTL و تم دوگانه؛ حجم باندل کوچک (مهم برای موبایل‌های افغانستان)؛ پالت سفارشی لاجورد/زعفرانی.

---

## ADR-004 — مسیریابی: HashRouter + `base: './'`
**وضعیت:** پذیرفته

**دلیل:** خروجی استاتیک روی هر هاستی (GitHub Pages، فایل لوکال، WebView اندروید) بدون تنظیم سرور کار می‌کند.

---

## ADR-005 — فونت: Vazirmatn باندل‌شده لوکال
**وضعیت:** پذیرفته

**تصمیم:** فقط وزن‌های 400/500/700 از پکیج npm با `@font-face` باندل می‌شود.

**دلیل:** شرط آفلاین کامل — هیچ CDN ای مجاز نیست؛ Vazirmatn پشتیبانی عالی دری/پشتو و ارقام فارسی (ss01) دارد.

---

## ADR-006 — ساختار آزمون کامل: ۱۶۰ سؤال / ۱۸۰ دقیقه
**وضعیت:** پذیرفته (قابل تنظیم)

**تصمیم:** blueprint آزمون کامل در `subjects.ts` به‌صورت داده تعریف می‌شود، نه هاردکد — تا با تغییر ساختار رسمی کانکور فقط یک فایل عوض شود.

---

## ADR-007 — مهاجرت schema به ساختار native سؤالات واقعی (رهنمون ۱۴۰۵)
**تاریخ:** ۱۴۰۵/۰۴/۲۱ · **وضعیت:** پذیرفته

**تصمیم:** با دریافت `questions.json` واقعی (۵۷ سؤال رهنمون ۱۴۰۵ با ساختار `question/options[].correct/why_correct/why_wrong/explanation/chapter_ref/verified`)، مدل داده (`schema.ts`) عیناً با این ساختار یکی شد — نه یک لایهٔ adapter بین دو فرمت. بانک نمونهٔ ۶۵ سؤالی قبلی (فرمت `stem/correctIndex/studyHint`) حذف شد.

**تغییرات کلیدی:**
- `correctIndex` دیگر ذخیره نمی‌شود؛ همیشه با `getCorrectIndex(q) = options.findIndex(o => o.correct)` محاسبه می‌شود.
- `SubjectId`/`Difficulty` از enum ثابت به `string` تغییر کرد؛ فهرست مضامین (`SUBJECTS`) در `subjects.ts` با `computeSubjects()` از خودِ داده استخراج می‌شود، نه هاردکد. آیکون هر مضمون در یک نگاشت جدا (تزئینی، نه محتوای سؤال) نگه‌داری می‌شود تا مضامین ناشناس هم آیکون پیش‌فرض بگیرند.
- فیلتر `verified===true` فقط در build پروداکشن (`import.meta.env.PROD`) اعمال می‌شود؛ در dev همه سؤالات (برای بازبینی) دیده می‌شوند.
- `QuestionView` برای پاسخ غلط، هم `question.explanation` (چرا درست، درست است) و هم `option.why_wrong` گزینهٔ انتخابی کاربر را در یک کادر مجزا و برجسته (سرخ) نشان می‌دهد — این تمایز محصول عمداً حفظ شد.

**دلیل:** درخواست صریح «هیچ فیلد را هاردکد نکن؛ همه از questions.json خوانده شود تا بچ‌های بعدی بدون تغییر کد اضافه شوند» با یک منبع واحد حقیقت بهتر برآورده می‌شود تا نگه‌داری دو فرمت موازی (ریسک ناهم‌خوانی، پیچیدگی adapter). چون بانک نمونهٔ قبلی صرفاً placeholder توسعه بود (نه محتوای واقعی کانکور)، حذف آن هزینه‌ای نداشت.

**بدیل ردشده:** لایهٔ adapter بین دو schema — پیچیدگی بیشتر بدون فایدهٔ واقعی، چون دادهٔ نمونه قبلی قرار نبود در پروداکشن بماند.

---

## ADR-008 — ابزار ادغام بچ (`scripts/merge-batch.mjs`) به‌جای نُرمال‌سازی در runtime
**تاریخ:** ۱۴۰۵/۰۴/۲۱ · **وضعیت:** پذیرفته

**زمینه:** بچ دوم سؤالات (۱۳ سؤال ریاضی) با شکل خام کاملاً متفاوتی رسید: `options` آرایهٔ رشته + `correct_index` عددی (نه آبجکت‌های `{text,correct,...}`)، `chapter` جدا از `topic`، `why`/`why_wrong[]` (نه `explanation`/`why_correct`/`why_wrong` روی هر گزینه)، و بدون wrapper سطح‌بالا. همچنین نام‌های هم‌معنیِ متفاوت: مضمون «ریاضی» به‌جای «ریاضیات»، سختی «سخت» به‌جای «دشوار».

**تصمیم:** به‌جای افزودن پیچیدگی به مسیر runtime اپ (تشخیص شکل + نُرمال‌سازی هر بار که اپ لود می‌شود)، یک اسکریپت Node مستقل (`scripts/merge-batch.mjs`) ساخته شد که:
1. شکل خام ورودی را تشخیص می‌دهد (canonical یا flat/correct_index)
2. به schema.ts's `Question` نُرمال می‌کند (`getCorrectIndex` دیگر لازم نیست چون correct روی گزینه‌ها ست می‌شود)
3. نام‌های هم‌معنی مضمون/سختی را یکدست می‌کند (`SUBJECT_ALIASES`, `DIFFICULTY_ALIASES`) تا در UI به‌جای دو چیپ جدا («ریاضی» و «ریاضیات») یک بانک یکدست دیده شود
4. اعتبارسنجی می‌کند (دقیقاً یک گزینه correct، فیلدهای اجباری) و تکراری‌ها را بر اساس id رد می‌کند
5. مستقیم در `src/data/bank/questions.json` merge و ذخیره می‌کند

**دلیل:** `questions.json` منبع واحد حقیقتِ *اپ* است (ADR-007)، اما منبع واحد حقیقتِ *تولیدکنندگان محتوا* نیست — بچ‌های خام از منابع/قالب‌های مختلف می‌رسند. جدا کردن «نُرمال‌سازی زمان ادغام» (یک‌بار، در اسکریپت) از «کد زمان اجرای اپ» (همیشه یک شکل ساده) هم کد اپ را ساده نگه می‌دارد و هم هر بچ جدید با شکل ناآشنا فقط به یک تابع normalize کوچک در همین اسکریپت نیاز دارد، نه تغییر در schema.ts یا کامپوننت‌ها.

**بدیل ردشده:** لایهٔ نُرمال‌سازی در `bank/index.ts` که هر بار اپ بارگذاری می‌شود شکل هر سؤال را حدس بزند — کندتر، پیچیده‌تر برای دیباگ، و باگ در نُرمال‌سازی مستقیم روی کاربر اثر می‌گذارد نه روی مرحلهٔ آماده‌سازی داده.

---

## ADR-009 — پوشه `content/questions` + اعتبارسنجی سخت‌گیرانه به‌عنوان دروازه کیفیت
**تاریخ:** ۱۴۰۵/۰۴/۲۲ · **وضعیت:** پذیرفته

**تصمیم:** فایل خام هر بچ سؤال در `content/questions/` نگه‌داری می‌شود و `scripts/merge-batch.mjs` بدون آرگومان اجرا می‌شود: خودش فایل‌های جدید یا تغییرکرده را (با مقایسه هش SHA-256 در `content/questions/.merged.json`) پیدا و ادغام می‌کند. اعتبارسنجی از «هشدار» به **دروازه** ارتقا یافت — هر سؤالی که یکی از این شرط‌ها را نداشته باشد وارد بانک نمی‌شود و با دلیل دقیق گزارش می‌شود:

- `correct_index` عددیِ غیر null و داخل محدوده گزینه‌ها (یا در شکل canonical: دقیقاً یک `correct:true`)
- دقیقاً **۴ گزینه** (`REQUIRED_OPTION_COUNT`)
- `verified === true`
- `explanation`، `chapter_ref`، متن هر گزینه، و `why_correct`/`why_wrong` همه موجود

**دلیل:** خط دفاع اول کیفیت، انسانی است (کسی که بچ را تهیه می‌کند). خط دوم باید ماشینی و غیرقابل‌دورزدن باشد، چون یک سؤال با جواب درستِ نامشخص در یک اپ آمادگی کانکور، بدتر از نبودن آن سؤال است. آرشیو خام در `content/questions/` باعث می‌شود منشأ هر سؤال قابل‌ردیابی بماند و ادغام مجدد (idempotent) بی‌خطر باشد.

**نکته اجرایی:** یک سؤال از قبل در بانک (`rahnamoon-1405-r1-q21`) با `verified:false` باقی مانده — عمداً برای بازبینی انسانی نگه داشته شده و در پروداکشن (`import.meta.env.PROD`) به کاربر نمایش داده نمی‌شود. با دروازه جدید، چنین سؤالی دیگر از مسیر ادغام وارد نمی‌شود.

**بدیل ردشده:** پذیرفتن سؤال ناقص با برچسب هشدار — ریسک می‌کرد سؤال بی‌جوابِ درست به دست دانش‌آموز برسد؛ رد کامل، شکست را زودتر و در جای ارزان‌تر (زمان ادغام) آشکار می‌کند.

---

## ADR-010 — `topic` (دستهٔ درشت) در برابر `chapter_ref` (زیرمبحث ریز)
**تاریخ:** ۱۴۰۵/۰۵/۱۴ · **وضعیت:** پذیرفته

**زمینه:** بازبینی دو-محورهٔ کد (محور Spec) کشف کرد که `normalizeFlatShape` در
`scripts/merge-batch.mjs` مقدار `chapter` را در **هر دو** فیلد `topic` و `chapter_ref`
می‌گذاشت، در حالی که بچ‌های خام هر دو را جدا دارند (`topic: "مشتق"` و
`chapter: "مشتق چندجمله‌ای درجه اول"`). `topic` درشت دور ریخته می‌شد.

**اثر اندازه‌گیری‌شده (پیش از اصلاح):** ۱۱۶ سؤال → **۱۰۲ مبحث مجزا، ۹۰ تک‌سؤالی**؛
ریاضیات ۶۵ سؤال → ۵۵ مبحث؛ «مشتق» زیر ۷ رشتهٔ متفاوت پخش شده بود. یعنی الزام ۳
(«نتایج را بر اساس subject و بعد topic گروپ کن») به ازای هر سؤال یک چیپ می‌ساخت و
فیلتر مبحث در الزام ۵ حدود ۱۰۰ چیپ بی‌فایده نشان می‌داد.

**تصمیم:** این دو فیلد نقش متفاوت دارند و هرگز یکی نمی‌شوند:
- `topic` = **دستهٔ درشت**؛ گروه‌بندی کارنامه و فیلتر بانک با آن کار می‌کنند.
- `chapter_ref` = **زیرمبحث ریز**؛ فقط برای «پیشنهاد مطالعه».

نگاشت اصلاح شد به `topic: q.topic || q.chapter` و `chapter_ref: q.chapter || q.chapter_ref || q.topic`،
و `topic` به دروازهٔ اعتبارسنجی (ADR-009) اضافه شد. ۵۹ سؤال قبلاً ادغام‌شده یک‌بار از
روی فایل‌های خام `content/questions/` بازنگاشت شدند.

**نتیجهٔ اندازه‌گیری‌شده (پس از اصلاح):** ۱۰۲ → **۷۵ مبحث**، تک‌سؤالی‌ها ۹۰ → ۵۴،
ریاضیات ۵۵ → ۳۲ مبحث، و «مشتق» یک دستهٔ واحد با ۷ سؤال شد.

**باقی‌مانده (خارج از این ADR):** ۵۷ سؤال شکل canonical (`batch0_core_57`) خودشان
`topic` ریزدانه دارند (مثل «گراف توابع — انتقال») و املای ناهمسان («انتیگرال»/«انتگرال»).
این مشکل **داده** است نه کد، و به تکت wayfinder «نگاشت سؤالات موجود به نصاب» واگذار می‌شود.

---

## ADR-011 — ارتقای خودکار `BANK_VERSION` هنگام ادغام
**تاریخ:** ۱۴۰۵/۰۵/۱۴ · **وضعیت:** پذیرفته

**تصمیم:** `scripts/merge-batch.mjs` پس از هر ادغام موفق، خودش `BANK_VERSION` را در
`src/data/bank/index.ts` یک واحد بالا می‌برد (به‌جای چاپ یادآوری برای ویرایش دستی).

**دلیل:** قید کاربر «بچ‌های بعدی سؤالات بدون تغییر کد اضافه شوند» با ویرایش دستیِ یک
ثابت در فایل TypeScript نقض می‌شد. بدتر: فراموش‌کردن آن باعث می‌شد seed دوباره اجرا نشود و
کاربرانِ فعلی سؤالات جدید را **هرگز نبینند** — شکستی خاموش.

**بدیل ردشده:** هش محتوای بانک به‌جای شماره نسخه — تغییر بزرگ‌تری در `seed.ts` می‌خواست
بدون سود عملی، چون شمارنده هم همان کار را می‌کند.

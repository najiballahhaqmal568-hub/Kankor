# 🗺️ نقشه: شالودهٔ دادهٔ برنامه‌ریز + پیش‌بینی قبولی

> `wayfinder:map` · tracker محلی markdown (در این محیط issue tracker نصب نبود)
> تکت‌ها: `wayfinder/tickets/`. تکت باز = فایلی که `status: open` دارد.

## Destination

شالودهٔ دادهٔ **مشترکِ** برنامه‌ریز و پیش‌بینی قفل شود: فورمول نمره‌دهی، مدل دادهٔ نصاب،
و منبع وزن مضامین تصمیم‌گیری‌شده باشند — طوری که لایهٔ ۲ (تولید تقویم) و لایهٔ ۳
(پیش‌بینی نمره) هر دو بتوانند بدون هیچ تصمیم بازی وارد فاز ساخت شوند.

## Notes

- **دامنه:** فقط تصمیم‌های داده‌ای. این نقشه چیزی نمی‌سازد — خروجی‌اش تصمیم است نه کد.
- **منابع:** `FEATURE_SPEC_planner_prediction.md` (§۳.۱، §۳.۳، §۴.۴، §۶)،
  `kankor_1405_cutoffs.json` (۳۳۴ رشته-دانشگاه، ۱۳ فیلد)، `DECISIONS.md` (ADR-002 تا ۰۰۹)
- **وضعیت فعلی اپ:** بانک ۱۱۶ سؤال · Dexie/IndexedDB · schema در `src/data/schema.ts`
- **قاعدهٔ صداقت (از spec §۴.۴):** هرگز وعدهٔ قطعی قبولی؛ همیشه بازه، همیشه ذکر سال مرجع.
- هر جلسه فقط **یک تکت** را حل می‌کند (به‌جز تکت‌های research).

## Decisions so far

<!-- یک خط برای هر تکت بسته‌شده -->

- [قواعد رسمی فورم و نمره‌دهی کانکور](tickets/001-qawaid-form-numradehi.md) — سقف ۳۶۰ و کوانتوم
  ۰٫۱ روی ۶۶۸ نمرهٔ واقعی اثبات شد؛ و **فرض §۴.۴ spec رد شد**: توزیع یکنواخت رقم دهم
  (chi²=۵٫۲) ثابت می‌کند نمره «تعداد درست × ۲٫۲۵» نیست — چیز دیگری (نمرهٔ مکتب؟ ضریب
  مضمون؟) در فورمول هست. وب فورمول را منتشر نکرده → به تکت *منبع قطعی فورمول نمره‌دهی* واگذار شد.

## Not yet specified

- **انتخاب سؤال برای آزمون تشخیصی اولیه** (§۲، ~۴۰ سؤال مخلوط) — تا مدل نصاب و نگاشت
  سؤالات روشن نشود نمی‌شود گفت «مخلوط» یعنی چه.
- **ضریب ضعف شاگرد** (§۳.۳) — فرمول تبدیل نتیجهٔ تشخیصی به سهم وقت هر مضمون.
- **سهمیهٔ ولایتی در پیش‌بینی** — دادهٔ فعلی فقط cutoff عمومی است. تحقیقِ تکت *قواعد رسمی
  فورم و نمره‌دهی* شاهد تازه‌ای آورد که این را **جدی‌تر** می‌کند: طبق گزارش سلام وطندار،
  نمرات ۱۵۳ و ۱۶۸ و ۱۷۴ سهمیه‌ای از نمرات ۳۰۰ و ۳۰۲ و ۳۲۲ عمومی پیشی گرفته‌اند. یعنی
  پیش‌بینیِ مبتنی بر cutoff عمومی می‌تواند برای شاگرد سهمیه‌ای **به‌شدت گمراه‌کننده** باشد.
  هنوز ticket نمی‌شود چون معلوم نیست دادهٔ سهمیه اصلاً در دسترس هست یا نه — این بخشی از
  چک‌لیست تکت *منبع قطعی فورمول نمره‌دهی* است.
- **میانگین متحرک چندساله** — وقتی فایل cutoff سال‌های دیگر برسد، قاعدهٔ ترکیبشان.

## Out of scope

این نقشه فقط شالودهٔ داده است؛ موارد زیر عمداً بیرون‌اند و برنمی‌گردند مگر مقصد بازتعریف شود:

- UI آنبوردینگ و داشبورد پیشرفت (§۲، §۴.۱)
- هشدارها، نوتیفیکیشن و بازچینش خودکار (§۴.۲)
- هویت انگیزشی و پیام‌های نقطه عطف (§۴.۳)
- داستان‌های موفقیت و فورم رضایت (§۴.۵)
- حالت «ماه سرنوشت‌ساز» (§۵)
- **ساخت واقعی** هر یک از لایه‌ها — این نقشه فقط تصمیم تولید می‌کند

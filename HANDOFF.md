# 🎓 سند انتقال پروژه — اپ آمادگی کانکور افغانستان

> این فایل برای **ادامهٔ پروژه توسط یک دستیار/توسعه‌دهندهٔ دیگر** نوشته شده.
> خودکفاست: اگر فقط همین یک فایل را بخوانی، می‌دانی پروژه چیست، کجاست، چه ساخته شده،
> چه قواعدی را نباید بشکنی، و قدم بعدی چیست.
> آخرین به‌روزرسانی: ۱۴۰۵/۰۵/۱۴ (2026-08-06)

---

## ۱. پروژه در یک نگاه

اپ **آمادگی کانکور افغانستان** — یک PWA کاملاً آفلاین، به زبان **دری (RTL)**، با تم تاریک
پیش‌فرض و پالت **لاجورد + زعفرانی**.

**فاز ۱ (MVP آفلاین) کامل و منتشر شده است** و شامل چهار قابلیت است:

1. **بانک سؤالات** — مرور و تمرین با فیلتر مضمون/مبحث/سختی
2. **شبیه‌ساز آزمون** — با تایمر، پرچم‌گذاری، ادامهٔ آزمون نیمه‌کاره
3. **کارنامهٔ هوشمند** — تحلیل مضمون‌به‌مضمون، توضیح هر گزینه، پیشنهاد مطالعه
4. **مرور فاصله‌دار** — الگوریتم SM-2

### تمایز اصلی محصول (این را نشکن)

برای **هر جواب غلط**، اپ دو چیز جدا نشان می‌دهد:
- (الف) `question.explanation` — چرا جواب درست، درست است
- (ب) `option.why_wrong` گزینه‌ای که **کاربر انتخاب کرده** — چرا انتخاب خودش غلط بود

بند (ب) همان چیزی است که رقبا ندارند. در `src/components/QuestionView.tsx` پیاده شده،
در یک کادر مجزا و برجسته (سرخ). هر بازنویسی UI باید این را حفظ کند.

---

## ۲. دسترسی‌ها

| چیز | آدرس |
|---|---|
| مخزن | `najiballahhaqmal568-hub/Kankor` |
| برنچ کاری | `claude/afghan-concourse-prep-app-66vlst` (برنچ `main` وجود ندارد) |
| اپ زنده | https://najiballahhaqmal568-hub.github.io/Kankor/ |
| انتشار | خودکار با `.github/workflows/deploy-pages.yml` روی هر push |

انتشار GitHub Pages یک‌بار به‌صورت دستی فعال شده (Settings → Pages → Source: **GitHub Actions**).
اگر workflow با خطای `Get Pages site failed: Not Found` شکست، همین تنظیم خاموش شده است.

---

## ۳. وضعیت فعلی (چه هست، چه نیست)

### ساخته شده ✅
- هر ۷ مرحلهٔ فاز ۱ + انتشار
- بانک سؤالات واقعی رهنمون ۱۴۰۵
- ابزار ادغام بچ‌های سؤال با اعتبارسنجی سخت‌گیرانه
- ۱۹ تست واحد در ۳ فایل (`sm2`, `examEngine`, `analytics`) — همه سبز
- `npm run build` و `tsc --noEmit` پاک

### بانک فعلی
```
ریاضیات ۶۵ · کیمیا ۲۳ · فزیک ۱۵ · بیولوژی ۱۳   =  ۱۱۶ سؤال
BANK_VERSION = 8 · یک سؤال verified:false (rahnamoon-1405-r1-q21، عمداً برای بازبینی انسانی)
```

### ⚠️ بزرگ‌ترین شکاف فعلی — محتوا
بانک **فقط ۴ مضمون ساینسی** دارد. دری، پشتو، انگلیسی، دینیات، تاریخ و جغرافیه
**هیچ سؤالی ندارند**. نتیجه: «آزمون کامل» فعلاً شبیه‌سازی واقعی کانکور هیچ رشته‌ای نیست
(نه ۱۶۰ سؤال است، نه توزیع واقعی دارد، نه مضامین انسانی را پوشش می‌دهد).

هر کار روی برنامه‌ریز و پیش‌بینی قبولی روی همین شالوده سوار است، و خود
`FEATURE_SPEC_planner_prediction.md` آن‌ها را به **۳۰۰+ سؤال** گره زده.
**افزودن بچ‌های سؤال، پرارزش‌ترین کار بعدی است.**

---

## ۴. Stack و دلایلش

| لایه | انتخاب | چرا |
|---|---|---|
| بیلد | Vite 5 | سریع، خروجی استاتیک |
| UI | React 18 + TypeScript (strict) | — |
| مسیریابی | react-router-dom با **HashRouter** + `base: './'` | روی هر هاست استاتیک بدون تنظیم سرور کار می‌کند |
| ذخیره‌سازی | **Dexie / IndexedDB** | معادل SQLite در وب؛ فقط وضعیت کاربر |
| محتوا | **JSON استاتیک** باندل‌شده | افزودن سؤال = یک PR ساده |
| PWA | vite-plugin-pwa (Workbox) | آفلاین کامل + نصب روی موبایل |
| استایل | CSS خالص + متغیر، بدون فریمورک | کنترل کامل RTL و تم دوگانه، باندل کوچک |
| فونت | **Vazirmatn لوکال** از node_modules | آفلاین شرط سخت است — هیچ CDN مجاز نیست |
| تست | Vitest + fake-indexeddb | — |

Node v22 · npm 10. مسیر آیندهٔ اندروید: بسته‌بندی با Capacitor (فاز ۲).

---

## ۵. ساختار پروژه

```
Kankor/
├── PROJECT_NOTEBOOK.md   ← دفترچهٔ زندهٔ پروژه (وضعیت، نکات، قواعد)
├── ROADMAP.md            ← نقشه راه با چک‌باکس، مرحله‌به‌مرحله
├── DECISIONS.md          ← ADR-001 تا ADR-011
├── HANDOFF.md            ← همین فایل
├── FEATURE_SPEC_planner_prediction.md   ← spec قابلیت آینده (برنامه‌ریز + پیش‌بینی)
├── kankor_1405_cutoffs.json             ← ۳۳۴ رشته-دانشگاه، نمرات کت‌آف ۱۴۰۵
├── content/questions/    ← فایل‌های خام بچ‌های سؤال + README جریان کاری
│   └── .merged.json      ← هش SHA-256 فایل‌های پردازش‌شده
├── scripts/merge-batch.mjs   ← ابزار ادغام + دروازهٔ اعتبارسنجی
├── wayfinder/            ← نقشهٔ تصمیم‌ها + ۷ تکت
└── src/
    ├── data/
    │   ├── schema.ts         ← تایپ‌ها + getCorrectIndex()
    │   ├── subjects.ts       ← استخراج پویای مضامین از داده
    │   └── bank/
    │       ├── index.ts      ← لودر + BANK_VERSION + validateBank()
    │       └── questions.json ← منبع واحد حقیقت محتوا
    ├── db/       ← Dexie: db, seed, questions, examSession, reviewQueue
    ├── logic/    ← sm2.ts · examEngine.ts · analytics.ts (+ تست‌ها)
    ├── lib/      ← format.ts (faNum…) · theme.ts · settings.ts
    ├── components/QuestionView.tsx   ← کامپوننت مشترک نمایش سؤال
    ├── pages/    ← Home · Bank · ExamSetup · ExamRunner · Report · Review · Settings
    └── styles/   ← tokens.css · base.css · components.css · fonts.css
```

---

## ۶. مدل داده

منبع واحد حقیقت محتوا: `src/data/bank/questions.json`.
`src/data/schema.ts` **عیناً** همان ساختار است — هیچ لایهٔ adapter و هیچ فیلد اضافه‌ای وجود ندارد (ADR-007).

```ts
export type SubjectId = string;   // ← عمداً enum نیست
export type Difficulty = string;  // ← عمداً enum نیست

export interface QuestionOption {
  text: string;
  correct: boolean;
  why_correct?: string;   // فقط روی گزینهٔ درست
  why_wrong?: string;     // فقط روی گزینه‌های غلط
}

export interface Question {
  id: string;
  subject: SubjectId;
  topic: string;        // دستهٔ درشت — گروه‌بندی کارنامه و فیلتر بانک
  difficulty: Difficulty;
  question: string;
  options: QuestionOption[];   // دقیقاً ۴ تا؛ دقیقاً یکی correct=true
  explanation: string;         // چرا جواب درست، درست است
  chapter_ref: string;         // زیرمبحث ریز — فقط «پیشنهاد مطالعه»
  language: string;
  source: string;
  verified: boolean;
}

/** اندیس گزینهٔ درست هرگز ذخیره نمی‌شود — همیشه محاسبه می‌شود */
export function getCorrectIndex(q: Question): number {
  return q.options.findIndex((o) => o.correct);
}
```

وضعیت کاربر در IndexedDB (Dexie): `attempts` · `answers` · `srsCards` · `activeExam` · `meta`.

### `topic` در برابر `chapter_ref` (ADR-010 — این را حتماً بخوان)

این دو **هرگز نباید یکی شوند**:

| فیلد | نقش | مثال |
|---|---|---|
| `topic` | دستهٔ **درشت**؛ کارنامه و فیلتر با آن گروه می‌شوند | `مشتق` |
| `chapter_ref` | زیرمبحث **ریز**؛ فقط پیشنهاد مطالعه | `مشتق چندجمله‌ای درجه اول` |

یک‌بار اسکریپت ادغام مقدار ریز را در هر دو می‌گذاشت؛ نتیجه ۱۰۲ مبحث برای ۱۱۶ سؤال بود
(۹۰ تک‌سؤالی) و گروه‌بندی کارنامه عملاً بی‌معنی شده بود. اصلاح شد → ۷۵ مبحث.

---

## ۷. جریان کاری محتوا (مهم‌ترین بخش عملیاتی)

**افزودن بچ سؤال جدید — بدون هیچ تغییر کد:**

```bash
# ۱. فایل خام را در content/questions/ بگذار (نام توصیفی: batch7_dari.json)
# ۲. از ریشهٔ پروژه:
node scripts/merge-batch.mjs
# ۳.
npm run test && npm run build
# ۴. commit + push → انتشار خودکار
```

اسکریپت خودش: فایل‌های جدید/تغییرکرده را با هش پیدا می‌کند · شکل خام را تشخیص و
نُرمال می‌کند · نام‌های هم‌معنی را یکدست می‌کند · اعتبارسنجی می‌کند ·
تکراری‌ها را رد می‌کند · `BANK_VERSION` را **خودکار** بالا می‌برد.

### دو شکل خام شناخته‌شده
- **A (canonical):** `{ schema_version, questions: [ { options: [{text, correct, why_correct?, why_wrong?}] } ] }`
- **B (flat):** آرایهٔ ساده با `options: string[]` + `correct_index` + `why` + `why_wrong[]` + `chapter`

اگر بچی با **شکل سوم** رسید: فقط یک تابع `normalize` دیگر به همین اسکریپت اضافه کن.
**کد اپ هرگز نباید تغییر کند.**

### دروازهٔ اعتبارسنجی (خط دفاع دوم کیفیت — ADR-009)
سؤالی که هرکدام را نداشته باشد **وارد بانک نمی‌شود** و با دلیل دقیق گزارش می‌شود:
- جواب درست مشخص (`correct_index` غیر null و در محدوده / دقیقاً یک `correct:true`)
- دقیقاً **۴ گزینه**
- `verified === true`
- `id`، `subject`، `topic`، `question`، `explanation`، `chapter_ref` خالی نباشند
- هر گزینهٔ غلط `why_wrong` و گزینهٔ درست `why_correct` داشته باشد
- طول `why_wrong` با تعداد گزینه‌ها برابر باشد

فلسفه: «یک سؤال با جواب درستِ نامشخص در اپ آمادگی کانکور، بدتر از نبودن آن سؤال است.»

### محدودیت شناخته‌شده
تکراری‌ها بر اساس `id` رد می‌شوند، پس **اصلاح یک سؤال موجود از طریق بچ منتقل نمی‌شود**.
فعلاً باید مستقیم در `questions.json` ویرایش شود. (اگر لازم شد، یک پرچم `--overwrite` اضافه کنید.)

---

## ۸. قواعد غیرقابل‌مذاکره

اینها بارها با زحمت به دست آمده‌اند؛ شکستنشان هزینه دارد:

1. **هیچ فیلدی هاردکد نشود.** همه‌چیز از `questions.json` خوانده شود تا بچ‌های بعدی
   بدون تغییر کد اضافه شوند. مضمون و سختی `string`اند نه enum؛ فهرست مضامین با
   `computeSubjects()` از خود داده استخراج می‌شود.
2. **RTL:** کل سند `dir="rtl"` است. در CSS فقط خواص logical
   (`inset-inline`, `padding-inline`, `border-inline-start`) — هرگز `left`/`right`.
3. **ارقام:** هر عدد نمایشی باید از `faNum()` / `faPercent()` / `faDuration()` /
   `faDate()` در `src/lib/format.ts` عبور کند (ارقام فارسی).
4. **رنگ:** توکن‌ها در `src/styles/tokens.css`. هرگز رنگ خام در کامپوننت‌ها.
5. **آفلاین شرط سخت است.** هیچ CDN، هیچ فونت/اسکریپت/تصویر خارجی. همه‌چیز باندل شود.
6. **هم‌معنی‌ها فقط در یک جا:** `SUBJECT_ALIASES` / `DIFFICULTY_ALIASES` تنها در
   `scripts/merge-batch.mjs`. هیچ نام هم‌معنی («ریاضی» به‌جای «ریاضیات») نباید در
   `subjects.ts`، کامپوننت‌ها یا fixture تست‌ها تکرار شود (ADR-008).
7. **عدد ثابت در JSX ننویس.** «۱۶۰ سؤال · ۱۸۰ دقیقه» یک‌بار هاردکد شده بود در حالی که
   آزمون واقعاً ۱۱۶ سؤال بود — کارت به کاربر دروغ می‌گفت. از `FULL_EXAM_TOTAL` و
   `FULL_EXAM_DURATION_SEC` بخوان.
8. **سه فایل ماندگار همیشه به‌روز بمانند:** `PROJECT_NOTEBOOK.md`، `ROADMAP.md`،
   `DECISIONS.md`. هر تصمیم مهم یک ADR جدید با دلیل و بدیل ردشده می‌گیرد.

---

## ۹. تصمیم‌های ثبت‌شده (جزئیات در `DECISIONS.md`)

| ADR | موضوع |
|---|---|
| 001 | پلتفرم: PWA (فاز ۲: Capacitor → APK) |
| 002 | JSON استاتیک + IndexedDB/Dexie |
| 003 | CSS خالص با متغیر، بدون فریمورک |
| 004 | HashRouter + `base: './'` |
| 005 | فونت Vazirmatn باندل‌شده لوکال |
| 006 | بلوپرینت آزمون به‌صورت داده، نه هاردکد |
| 007 | schema عیناً منطبق بر questions.json (بدون adapter) |
| 008 | نُرمال‌سازی شکل خام در اسکریپت ادغام، نه در runtime اپ |
| 009 | اعتبارسنجی سخت‌گیرانه به‌عنوان دروازه (نه هشدار) |
| 010 | جداسازی `topic` (درشت) از `chapter_ref` (ریز) |
| 011 | ارتقای خودکار `BANK_VERSION` هنگام ادغام |

---

## ۱۰. کارهای باز

### الف) محتوا — بالاترین اولویت
بچ‌های سؤال برای مضامین انسانی (دری، پشتو، انگلیسی، دینیات، تاریخ، جغرافیه) و
تکمیل ساینس تا ۳۰۰+ سؤال. زیرساخت کاملاً آماده است.

### ب) قابلیت آینده: برنامه‌ریز + پیش‌بینی قبولی
spec کامل در `FEATURE_SPEC_planner_prediction.md` (v0.2/v0.3/v0.4). **هنوز ساخته نشده**
و عمداً به رسیدن بانک به ۳۰۰+ سؤال گره خورده.

قبل از ساخت، تصمیم‌های داده‌ای در `wayfinder/` باید بسته شوند:

| تکت | عنوان | وضعیت |
|---|---|---|
| ۰۰۱ | قواعد رسمی فورم و نمره‌دهی | ✅ بسته |
| ۰۰۲ | مدل دادهٔ نصاب | باز، بدون بلاک ← **frontier** |
| ۰۰۳ | منبع وزن مضامین هر رشته | بلاک‌شده توسط ۰۰۷ |
| ۰۰۴ | نگاشت سؤالات به نصاب | بلاک‌شده توسط ۰۰۲ |
| ۰۰۵ | از کارنامه تا نمرهٔ تخمینی | بلاک‌شده توسط ۰۰۷ |
| ۰۰۶ | محل و شکل ذخیرهٔ `plan_state` | باز، بدون بلاک ← **frontier** |
| ۰۰۷ | منبع قطعی فورمول نمره‌دهی | باز — **مالک پروژه فعلاً کنار گذاشت** |

**یافتهٔ مهم تکت ۰۰۱ (تجربی، اثبات‌شده):** فرض spec §۴.۴ که «نمره = تعداد درست × ۲٫۲۵»
باشد **رد شد**. روی ۶۶۸ نمرهٔ واقعی ۱۴۰۵: همه مضرب دقیق ۰٫۱ هستند و رقم دهم
توزیع یکنواخت دارد (chi²=۵٫۲ در برابر آستانهٔ ۱۶٫۹). اگر فورمول ساده بود فقط
{۰، ۳، ۵، ۸} در رقم دهم دیده می‌شد. یعنی چیز دیگری (نمرهٔ مکتب؟ ضریب مضمون؟)
در فورمول هست. وب آن را منتشر نکرده.

**چون ۰۰۷ کنار گذاشته شد**، تکت‌های ۰۰۳ و ۰۰۵ معلق می‌مانند مگر مسیر عوض شود:
به‌جای «نمرهٔ مطلق تخمینی»، پیش‌بینی بر اساس **رتبه/صدک** نسبت به
`kankor_1405_cutoffs.json` (که موجود است). این نه فورمول رسمی می‌خواهد نه فایل خام،
و با قاعدهٔ صداقت spec §۴.۴ («همیشه بازه، هرگز وعدهٔ قطعی قبولی، همیشه ذکر سال مرجع»)
سازگارتر است.

### ج) چهار تصمیم باز تکت ۰۰۲ (هنوز جواب داده نشده)
1. درخت نصاب **جدا تألیف** شود (`curriculum.json`) یا از سؤالات **استخراج** شود؟
   شواهد به تألیف جدا اشاره می‌کند: کیمیا ۲۳ سؤال/۱۸ مبحث، فزیک ۱۵/۱۳، بیولوژی ۱۳/۱۲ —
   استخراج نصاب نمی‌سازد. ضمناً املای ناهمسان زیاد است
   (`انتیگرال`/`انتگرال` · `کیمیای عضوی`/`کیمیای ارگانیک` · `مومنتم`/`مومنتم خطی`).
2. واحد مضامین حفظی: صفحه از کدام چاپ کتاب؟ چطور در برابر تغییر چاپ مقاوم شود؟
3. تفکیک صنف (۱۱ / ۱۲ / فارغ) کجای درخت بنشیند؟
4. مضامین تمرینی و حفظی یک مدل مشترک یا دو مدل جدا؟

### د) بدهی فنی کوچک
- شمارش مضامین از بانک در سه جا تکرار شده (`computeSubjects` در subjects.ts،
  `AVAILABLE` در ExamSetup.tsx، memo سختی در Bank.tsx) — یک `facets(questions)` مشترک بهتر است.

---

## ۱۱. دستورات

```bash
npm install
npm run dev       # سرور توسعه
npm run build     # tsc --noEmit + vite build
npm run test      # vitest run (۱۹ تست)
npm run preview   # پیش‌نمایش بیلد

node scripts/merge-batch.mjs            # ادغام همهٔ فایل‌های جدید content/questions
node scripts/merge-batch.mjs <path>     # ادغام یک فایل مشخص

# دیدن frontier نقشهٔ wayfinder:
grep -L "status: closed" wayfinder/tickets/*.md | xargs grep -l "blocked_by: \[\]"
```

---

## ۱۲. تله‌هایی که قبلاً خورده‌ایم

- **`topic` را با `chapter` قاطی نکن** — پرهزینه‌ترین باگ تا حالا (ADR-010).
- **`BANK_VERSION` را دستی ویرایش نکن** — خودکار است. اگر بالا نرود، seed دوباره اجرا
  نمی‌شود و کاربران فعلی سؤالات جدید را **هرگز نمی‌بینند** (شکست خاموش).
- **اعتبارسنجی دوگانه واگرا می‌شود** — `validateBank()` در `bank/index.ts` و `validate()`
  در اسکریپت ادغام باید همیشه یک قاعده داشته باشند (الان هر دو: دقیقاً ۴ گزینه).
- **مقایسهٔ بچ خام با بانک** — اگر خواستی بفهمی یک بچ چه تغییری آورده، `topic` خام را با
  `topic` ذخیره‌شده مقایسه کن، نه با `chapter_ref`. (یک‌بار همین اشتباه گزارش غلط ساخت.)
- **push رد می‌شود** اگر مالک از رابط وب گیت‌هاب فایلی آپلود کرده باشد →
  `git fetch && git rebase origin/<branch>` بعد push.

---

## ۱۳. پرامپت شروع برای دستیار بعدی

> این مخزن `najiballahhaqmal568-hub/Kankor` است — اپ آمادگی کانکور افغانستان
> (PWA آفلاین، دری/RTL، Vite + React + TypeScript + Dexie). روی برنچ
> `claude/afghan-concourse-prep-app-66vlst` کار کن.
>
> قبل از هر کاری این‌ها را بخوان: `HANDOFF.md` (نمای کامل)، بعد `PROJECT_NOTEBOOK.md`،
> `ROADMAP.md` و `DECISIONS.md`. این سه فایل ماندگارند و باید بعد از هر تغییر
> مهم به‌روز شوند.
>
> قواعد سختی که نباید بشکنی در بخش ۸ همان `HANDOFF.md` فهرست شده — خصوصاً:
> هیچ فیلدی هاردکد نشود (همه از `questions.json`)، CSS فقط با خواص logical،
> هر عدد نمایشی از `faNum()` عبور کند، و هیچ منبع خارجی/CDN (آفلاین شرط سخت است).
>
> کار من این است: ‹اینجا بنویس چه می‌خواهی›
